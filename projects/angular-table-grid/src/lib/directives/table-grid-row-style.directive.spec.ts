import { TableGridRowStyleDirective } from './table-grid-row-style.directive';

describe('TableGridRowStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new TableGridRowStyleDirective();
    expect(directive).toBeTruthy();
  });
});
